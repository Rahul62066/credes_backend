/**
 * Bot module — Service layer.
 * Handles bot-related logic (scheduled tasks, automations).
 */

export class BotService {
  // async triggerAction(type: string, payload: unknown) { ... }
  // async getStatus(botId: string) { ... }
}

export const botService = new BotService();
